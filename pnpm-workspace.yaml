packages:
  - "packages/*"
  - "packages/packs/*"
  - "apps/*"
# esbuild needs to run its install script to download the native binary that
# tsup uses to build the packages.
allowBuilds:
  esbuild: true
