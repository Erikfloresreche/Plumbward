{
  "name": "plumbward-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "vitest run",
    "test:unit": "vitest run src/",
    "test:e2e": "vitest run test/",
    "check:coherencia": "node scripts/verificar-coherencia.mjs",
    "check:mutations": "node scripts/check-mutations.mjs",
    "test:watch": "vitest",
    "cli": "node packages/cli/dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsup": "^8.3.5",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
